module.exports = function notificationHandlers(io, socket) {
  // User is already in their personal room (user:{userId}) from socketServer.js
  // Notifications are emitted directly from notification.service.js using getIO()
};
